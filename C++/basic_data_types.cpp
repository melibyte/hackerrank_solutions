#include <iostream>
#include <cstdio>

using namespace std;

int main() {
	int a;
	long l;
	char ch;
	float f;
	double d;
	scanf("%d %ld %c %f %lf", &a, &l, &ch, &f, &d);
	printf("%d \n %ld \n %c \n %f \n %lf", a, l, ch, f, d);
	return 0;
}
